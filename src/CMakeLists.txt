cmake_minimum_required(VERSION 2.8)

add_definitions(
    -std=c++11
    )

#include_directories(
#    )


#Handles linking (Via the libraries)
#link_libraries(
#    )

#Handles linking (Via directories)
#link_directories(
#)

add_executable(
		main
    main.cpp
    )

